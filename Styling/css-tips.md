# CSS Tips

## Absolute positioning relative to a parent

In order to position relative to a parent set the parent to `relative` and the child to `absolute`.

```html
<div class="relative">
  <div class="absolute"/>
</div>
```
